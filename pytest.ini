[pytest]
minversion = 6.0
addopts = 
    --cov=app 
    --cov-report=term-missing 
    --cov-branch 
    --cov-fail-under=80
    --strict-markers
testpaths = tests
python_files = test_*.py

filterwarnings =
    ignore::DeprecationWarning
